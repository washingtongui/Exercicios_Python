from random import randint
def soteio(lista):
    lista = list()
    for i in range(0, randint(5, 10)):
        numero = randint(1, 100)
        lista.append(numeros)

numeros = []
print(soteio(numeros))